import React from 'react';
import GoogleMap from './GoogleMap'; // Example, replace with actual Google Map component

const LocationMap = () => {
  return (
    <div className="location-map">
      <h2>Location</h2>
      <GoogleMap />
    </div>
  );
};

export default LocationMap;
